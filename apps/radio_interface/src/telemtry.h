// basic file operations
#include <iostream>
#include <fstream>
//#include<stdio.h>
#include <ei.h>
#include "erlcmd.h"

using namespace std;
void logData(const char *uid);
void send_data(const char *uid, size_t len);
